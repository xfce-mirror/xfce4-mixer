FIXED? fix bug occuring when entering profile manager, creating, leaving manager, entering manager, deleting profile
profile manager: mixer-profilebox-detail.gob: load/save (just auto-create profile/save profile)


device_changed() {
	cache_vc->refresh();
	current_profile->refresh_views();
	vc_profile_item_producer_produce(current_profile);
}

save keyboard shortcuts
