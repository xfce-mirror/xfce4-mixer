per-profile window size ?

device_changed() {
	cache_vc->refresh();
	current_profile->refresh_views();
	vc_profile_item_producer_produce(current_profile);
}

gtk_settings ?
restore last used profile on restart
set currently used profile as selected in the manager
